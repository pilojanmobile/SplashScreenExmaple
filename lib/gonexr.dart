import 'package:flutter/material.dart';

class Gonext extends StatelessWidget {
  const Gonext({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
          body: Container(
        alignment: Alignment.center,
        child: Text("ok"),
      ),
    );
  }
}